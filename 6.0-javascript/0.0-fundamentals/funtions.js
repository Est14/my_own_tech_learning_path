var name_Est = 'Esteban', age_Est = 28;
var name_Jos = 'Jose', age_Jos = 27;

function printAge(name, age){
    console.log(`${name} is ${age} years old`)

}

printAge(name_Est, age_Est);
printAge(name_Jos, age_Jos);

var name = 'Daniela'

function printNameInUpperCase(name){
    n = name.toUpperCase()
    console.log(n)
}

printNameInUpperCase(name)