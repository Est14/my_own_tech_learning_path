const cadena = "La nañana  oscura"

// Retorna true or false si la cadena comienza con algo
console.log(cadena.startsWith('La'));

// Igual que lo anterior pero si termina
console.log(cadena.endsWith('blanca'));
